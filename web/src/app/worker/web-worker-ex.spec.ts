// import { WebWorkerEx } from './web-worker-ex';
// import { TestBed, tick } from '@angular/core/testing';

describe('WebWorkerEx', () => {
  it('compress()', (done: DoneFn) => {
    // expect(new WebWorkerEx()).toBeTruthy();
    // let promise = WebWorkerEx.compress('text');
    // promise.then(v => {
    //   expect(v).toBeFalsy();
    //   done();
    // })
    done();
  });
});
