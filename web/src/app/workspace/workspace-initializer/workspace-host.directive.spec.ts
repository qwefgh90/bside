import { WorkspaceHostDirective } from './workspace-host.directive';

describe('WorkspaceHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkspaceHostDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
