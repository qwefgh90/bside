import { UserActionDispatcher } from './user-action-dispatcher';

describe('UserActionDispatcher', () => {
  it('should create an instance', () => {
    expect(new UserActionDispatcher()).toBeTruthy();
  });
});
