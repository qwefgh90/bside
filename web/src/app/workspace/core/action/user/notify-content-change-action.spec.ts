import { NotifyContentChangeAction } from './notify-content-change-action';

describe('NotifyContentChangeAction', () => {
  it('should create an instance', () => {
    expect(new NotifyContentChangeAction()).toBeTruthy();
  });
});
