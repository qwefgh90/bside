import { WorkspaceSelectAction } from './workspace-select-action';

describe('WorkspaceSelectAction', () => {
  it('should create an instance', () => {
    expect(new WorkspaceSelectAction()).toBeTruthy();
  });
});
