import { TabCloseMicroAction } from './tab-close-micro-action';

describe('TabCloseMicroAction', () => {
  it('should create an instance', () => {
    expect(new TabCloseMicroAction('')).toBeTruthy();
  });
});
