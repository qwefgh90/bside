import { EditorDirective } from './editor.directive';

describe('EditorDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
