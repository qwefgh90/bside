import { RouterLinkDirectiveStub } from './router-link-directive-stub';

describe('RouterLinkDirectiveStub', () => {
  it('should create an instance', () => {
    expect(new RouterLinkDirectiveStub()).toBeTruthy();
  });
});
